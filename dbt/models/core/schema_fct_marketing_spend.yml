version: 2

models:
  - name: fct_marketing_spend
    columns:
      - name: date
        tests: [not_null]
      - name: channel
        tests: [not_null]
      - name: spend_try
        tests: [not_null]
      - name: clicks
        tests: [not_null]
