name: "etl_pipeline_case"
version: "1.0.0"
config-version: 2
require-dbt-version: ">=1.7.0,<1.8.0"
profile: "elt_case"
model-paths: ["models"]

models:
  etl_pipeline_case:
    +materialized: view
    staging:
      +materialized: view
      +schema: stg
    core:
      +materialized: table
      +schema: core
    marts:
      +materialized: table
      +schema: marts
